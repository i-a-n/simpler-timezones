{
  "extends": "../tsconfig",
  "compilerOptions": {
    "declaration": true /* Generates corresponding '.d.ts' file. */,
    "emitDeclarationOnly": true,
    "outDir": "../distributions/types" /* Redirect output structure to the directory. */
  }
}
